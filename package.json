{
  "name": "shift-assistant",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs tests/solver/*.test.js",
    "test:browser": "node --test tests/*.browser.mjs"
  },
  "devDependencies": {
    "playwright": "latest"
  }
}
