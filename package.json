{
  "name": "shift-assistant",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:browser": "node --test tests/*.browser.mjs"
  },
  "devDependencies": {
    "playwright": "latest"
  }
}
